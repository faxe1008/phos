name: CI

on:
  push:
    branches: [master, main]
    tags: ['v*']
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  core:
    name: phos_core (analyze + test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: core
    steps:
      - uses: actions/checkout@v4

      - uses: dart-lang/setup-dart@v1
        with:
          channel: stable

      - name: Pub get
        run: dart pub get

      - name: Analyze
        run: dart analyze --fatal-infos

      - name: Test
        run: dart test

  app:
    name: app (analyze + test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.47.1

      - name: Pub get
        run: flutter pub get

      - name: Analyze
        run: flutter analyze --fatal-infos

      - name: Test
        run: flutter test

  apk:
    name: debug APK
    runs-on: ubuntu-latest
    needs: [core, app]
    defaults:
      run:
        working-directory: app
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.47.1
          cache: true

      - name: Build debug APK (arm64)
        run: flutter build apk --debug --target-platform android-arm64

      - uses: actions/upload-artifact@v4
        with:
          name: phos-debug-arm64
          path: app/build/app/outputs/flutter-apk/app-debug.apk
          if-no-files-found: error

  publish:
    name: publish release
    if: github.event_name != 'pull_request'
    needs: apk
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download APK
        uses: actions/download-artifact@v4
        with:
          name: phos-debug-arm64
          path: out

      - name: Resolve release name
        id: rel
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            # Tagged build: the tag is the release name.
            echo "name=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
          else
            # Branch build: name it after the build number from pubspec.
            build=$(sed -n 's/^version: .*+\([0-9][0-9]*\)$/\1/p' app/pubspec.yaml)
            echo "name=Build ${build}" >> "$GITHUB_OUTPUT"
            echo "prerelease=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish to GitHub Releases
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          apk=out/app-debug.apk
          if gh release view "${{ steps.rel.outputs.name }}" >/dev/null 2>&1; then
            gh release upload "${{ steps.rel.outputs.name }}" "$apk" --clobber
          else
            gh release create "${{ steps.rel.outputs.name }}" \
              --title "${{ steps.rel.outputs.name }}" \
              --notes "phos ${{ steps.rel.outputs.name }} — built from ${{ github.ref_name }} (${{ github.sha }})" \
              ${{ steps.rel.outputs.prerelease == 'true' && '--prerelease' || '' }} \
              "$apk"
          fi